/* ===================================================================
   Único arquivo que você precisa editar depois do deploy.

   Troque o endereço abaixo pelo endereço real do SISTEMA (o app onde as
   líderes entram). É ele que os links "Já participo — entrar" e "Criar
   meu acesso" usam.

   Sem barra no final.
   =================================================================== */
window.CAFE_CONFIG = {
  appUrl: "https://cafe-com-deus-shine.netlify.app",
};
